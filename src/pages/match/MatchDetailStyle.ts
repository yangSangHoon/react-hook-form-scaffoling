import { css, jsx } from '@emotion/react';

const MatchDetailStyle = css`
    border: 1px solid gray;
    padding: 20px;

    background-color: gray;
    & .error {
        color: red;
    }
`;

export default MatchDetailStyle;
